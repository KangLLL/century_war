using UnityEngine;
using System.Collections;

public class TestLogicConstructFinish : MonoBehaviour 
{
	void OnClick()
	{
		/*
		print("asadfasdfadfadf: " + LogicController.Instance.AvailableBuilderBuildingNO.Count);
		BuildingIdentity id = new BuildingIdentity();
		id.buildingType = ConfigUtilities.Enums.BuildingType.Barracks;
		id.buildingNO = 0;
		LogicController.Instance.FinishBuildingConstruct(id);
		print("mncvvcmvhncnvcb: " + LogicController.Instance.AvailableBuilderBuildingNO.Count);
		*/
	}
}
