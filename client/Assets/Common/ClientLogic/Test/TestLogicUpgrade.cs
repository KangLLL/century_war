using UnityEngine;
using System.Collections;

public class TestLogicUpgrade : MonoBehaviour 
{
	public void OnClick()
	{
		/*
		BuildingIdentity id = new BuildingIdentity();
		id.buildingType = ConfigUtilities.Enums.BuildingType.GoldMine;
		id.buildingNO = 0;
		LogicController.Instance.UpgradeBuilding(id, LogicController.Instance.AvailableBuilderBuildingNO[0]);
		Debug.Log("=============================!");
		*/
	}
}
